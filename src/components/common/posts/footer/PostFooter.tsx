import React from 'react';
import styled from 'styled-components';

const PostFooter: React.FC = () => {
  return <PostFooterContainer></PostFooterContainer>;
};

const PostFooterContainer = styled.div``;

export default PostFooter;
